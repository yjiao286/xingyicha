# 敏感词表 —— 格式示例（本文件只演示格式，不含任何真实词）
#
# 实际词表放在 .git/sensitive-terms.txt，**刻意不纳入版本管理**：
# 一份列出要拦的人名/客户名的文件，本身就是泄漏（初版把词表写进脚本，
# 结果被脚本自己抓了出来）。
# 新克隆的仓库没有这个词表，此时只剩 tools/check_sensitive.py 里的形态
# 匹配（手机号/身份证/统一社会信用代码/住址/邮箱）仍然生效。
#
# 一行一词；'#' 起注释；空行忽略；子串匹配（不需要写通配符）。
#
# 提示：本文件自身也会被钩子扫描，所以这里不要写任何形态逼真的号码或
# 证件号——真写成 xx 位数字，钩子会把它当成真值拦下（这是设计如此）。

# 真实人名（每个一行）
# <两到四个汉字>

# 真实公司名
# <公司全称或其中足以识别的片段>

# 项目名
# <项目全称或片段>

# 赛事 / 客户词汇
# <赛事名、主办方名、内部代号>

# 历史上泄漏过的具体值——号码、统一社会信用代码、住址等
# 一个字面照抄，子串匹配即可
