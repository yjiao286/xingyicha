# 桌面版（Windows / Linux / macOS）构建依赖，三平台共用
# 与 requirements.txt（Docker 部署）的差异：
#   - 去掉 gunicorn（Windows/macOS 不可用，桌面版统一用 waitress）
#   - 增加 waitress（生产级 WSGI 服务器，跨平台）
#   - 增加 pyinstaller / pillow（仅构建期使用，不进入运行时）
flask>=3.0
python-docx>=1.0
pypdf>=5.0
olefile>=0.46
openpyxl>=3.1
pymupdf>=1.24
rapidocr-onnxruntime>=1.3
waitress>=3.0
pyinstaller>=6.6
pillow>=10.0
